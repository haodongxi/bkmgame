# 宝可梦：关都篇（bkmGame）开发对话存档 — 2026-08-14

> 用途：本文件是本会话（Codex 桌面端）的完整工作记录，供用户在其他设备（如家中 Codex）恢复上下文。
> 项目：`/Users/haodongsheng/Documents/github/bkmGame`（纯文字像素宝可梦 RPG，原生 JS，无构建）
> 测试命令：
> - 逻辑冒烟：`/Users/haodongsheng/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node test/smoke_test.js`
> - 浏览器冒烟：先启动无头 Chrome `--remote-debugging-port=9222`，再 `node test/browser_play.js`
> 固定规则：新文件作者写 `Created by haodongsheng`；破坏性操作先确认；中文文案贴合动画味；提交后 push。

---

## 一、会话时间线（每条用户请求 → 处理 → 结果）

### 1. 无尽之塔每层属性提示
- 用户：每层属性建议要不要给克制关系提示。
- 处理：`towerThemeFor(floor)`（core.js）按 17 系克制表算克制建议；塔内进度弹窗 + 开战日志都显示「第 X 层（主题）！建议使用 XX 系招式克制！」；混合传说层无固定建议。
- 提交：`e8be6ae`。逻辑 271/271，浏览器 56/56。

### 2. 无尽之塔边界情况测试
- 用户：中途死亡、更换宝可梦等边界测了没。
- 处理：发现真 bug——通关后按钮写「重刷第 100 层」但点击无反应，改为从第 1 层重刷（保留称号与历史最佳）；补全 8 类边界测试：1 层无存档败北、全灭回存档点、塔内手动换人/拦截/倒下自动换、敌方 5 只连战、同归于尽按败北、5 倍层存档+发奖边界、穿绳回城保留进度。
- 提交：`d64cea4`。逻辑 296/296，浏览器 56/56。

### 3. 挑战条件
- 用户问无尽之塔挑战条件：需绿色徽章（8 徽章）、1 层 Lv48 起、塔内不可逃跑/捕捉/回城补给、全灭回存档点。

### 4. 宝可梦稀有度分级
- 用户：稀有/普通分级游戏内完全看不到，能否用词缀或颜色区分。
- 处理：`rarityOf()`（core.js）按捕获率 + 传说名单分四级：传说（★金色）/稀有（紫色）/少见（蓝色）/普通（不显示）；图鉴、队伍、电脑箱、详情、战斗卡片、野生开场日志全展示。
- 提交：`872be85`。浏览器 60/60。
- 后续用户反馈 UI 变形/裁剪：词缀改为纯彩色文字（去掉边框药丸盒），图鉴内词缀单独一行；新增「全图鉴 151 格横向无溢出」回归（桌面 + 390px 手机视口零溢出）。
- 提交：`b2722d4`。浏览器 62/62。

### 5. 宝可梦中心满血收费问题
- 用户：满血去城镇回复还要钱。
- 处理：`visitCenter` 改为只有实际需要恢复（掉血/异常/PP 未满）才收费；满血满 PP 免费并提示「精神满满」。
- 提交：`1c4cabd`。逻辑 313/313，浏览器 62/62。

### 6. 道具用途不明（月亮石）
- 用户：有的道具不知道干啥用。
- 处理：进化石描述动态补全——`stoneTargets()` 列出具体进化对象（如「让宝可梦进化：皮皮→皮可西」）；新增「📘 道具图鉴」弹窗（城镇/野外按钮，按五类列出全部道具含价格/持有数/说明）。
- 提交：`16b9d7d`。逻辑 321/321，浏览器 64/64。

### 7. 大晴天 / 高速移动是干嘛的
- 用户问两类技能用途。
- 结论：大晴天=战斗内改天气（晴 5 回合，火×1.5/水×0.5）；高速移动=自身速度↑2（影响先手）。但 1v1 下高速移动对高速怪是废招。

### 8. 招式效果说明 + 学习面平衡（方案 1+2）
- 用户：1v1 用高速移动有啥用；同意方案 1+2。
- 处理：
  - `moveEffectText()`（ui.js）：战斗按钮与详情面板显示效果说明（天气→晴、自身/对方能力↑↓、几率异常、回复比例、先制/束缚/连续攻击等）。
  - 学习面清理：全图鉴 151 只不再有任何宝可梦学高速移动（原 30 只高速怪 40~50 级位改为摔打/高速星星/大字爆炎/龙爪/自我再生/极光束等）；低速高攻怪走强化路线（卡比兽 35 级瞬间失忆、铁甲犀牛 15 级变硬）；生成模板同步更新。
- 提交：`2d6e1fb`。逻辑 328/328，浏览器 67/67。

### 9. 已学高速移动的旧存档怎么办 → 招式更换面板（方案 A'）
- 用户：挪走后已学习的玩家怎么办；不升级也学不了新招。
- 处理：详情面板「🔄 更换招式」——选栏位 → 可学清单（当前等级及以下、排除已学会）→ 替换；费用 500+等级×30；满级也能换掉残留废招；PP 对齐、随存档保存。
- 提交：`4f93c2d`。逻辑 339/339，浏览器 70/70。
- 排查插曲：浏览器冒烟首次触发原生 `confirm` 无 override 导致无头 Chrome 卡死，已给测试补 `window.confirm = function(){ return true; }`。

### 10. 睡觉（rest）技能修复
- 用户：比兽有个技能叫睡觉（指出异常）。
- 处理：确认 bug——rest 只有基础数据没有效果（用出来「什么都没有发生」）；实现完整机制：HP 回满 + 治愈异常 + 睡眠 1~3 回合（满血时无效），效果说明同步。
- 提交：`4ad31a7`。逻辑 342/342，浏览器 70/70。

### 11. 金币获取渠道
- 用户问金币来源。结论：战斗奖金（训练家 120~2400、道馆学徒、火箭队 900/500、宿敌、圣安奴号 1000、无尽之塔 层数×30、强盗 1600）+ 出售道具（金珠 5000 等）+ 初始 3000；野生战斗不给钱、馆主战不给钱。

### 12. 全图鉴投放补全（MVP14）——本次大工程
- 用户：呆呆兽/大舌贝/六尾怎么获取 → 检查后发现 75 只图鉴无获取途径。
- 处理：MVP14 三块投放全部落地：
  - 地图野生池补 ~46 只（按主题进 26 个节点，进化链自动闭环）；
  - 钓鱼池按水域扩充（`FISH_POOLS` + `fish()` 按节点取池：华蓝北出呆呆兽/蚊香蝌蚪/角金鱼，南部海域出墨海马/大钳蟹，双子岛冰水域出大舌贝/小海狮）；
  - 传说/化石：闪电鸟（24号雷阵雨 2%）、火焰鸟（21号水域 2%）、超梦（冠军之路 1%）、梦幻（21号雨 1%）、菊石兽/化石盔（月见山 2%）、化石翼龙（冠军之路 2%）。
  - 避开 route24 雨池（保迷你龙 5% 硬断言）。
- 提交：`1b84e53`。逻辑 354/354，浏览器 70/70。

### 13. 全部获取途径清单
- 用户要看全渠道。已生成完整清单（开局/野生池按地图/钓鱼按水域/交换/商人/火箭队/鲤鱼王大叔/进化链）。

### 14. 进化图鉴点亮修复
- 用户：小火龙升级到喷火龙，火恐龙/喷火龙有没有点亮。
- 处理：确认 bug——`evolveTo` 无图鉴登记，进化后形态图鉴空白；修复为进化统一登记「已见+已捕获」并提示；旧存档读档时队伍/电脑箱已有宝可梦自动回填点亮。
- 提交：`cb9bacf` + `c2d2574`。逻辑 359/359，浏览器 70/70。

### 15. 图鉴详情弹窗（方案 A）
- 用户：图鉴点击展示属性/技能介绍是否合理；并指出「只有点亮才能看获取途径是鸡肋」。
- 处理：`showDexDetail(id)`——未发现（???）保持神秘只显示 No.+"尚未遇见"；已见/已捕获显示属性、种族值（含总和）、捕获率、稀有度、进化路线、等级学习面（招式属性/威力/PP/效果）与完整获取途径（`acquisitionPaths()`，数据动态生成）。
- 提交：`51bf929`。逻辑 368/368，浏览器 76/76。

### 16. 伊布进化等级
- 用户问伊布多少级进化。结论：伊布不靠等级进化，用雷之石/水之石/火之石分别进化为雷伊布/水伊布/火伊布；建议 36 级学会舍身冲撞后再进化。

### 17. 队伍条点击直达详情
- 用户（附图，圈出宝可梦部分）：点击宝可梦直接进详情，不用去精灵队伍里点。
- 处理：地图顶部队伍条卡片加 `onclick="showMonDetail(i)"` + 悬停高亮。
- 提交：`fa363f2`。浏览器 77/77。

### 18. 地图「主要遭遇」与图鉴获取途径不一致
- 用户：列出的获取路径和地图主要遭遇对不上，是 bug 吗。
- 结论：不是 bug——地图弹窗 `topEncounters` 只显示全天气总权重前 3；图鉴途径列出全池（含 1~8% 稀有）。已用 5 号道路实例验证（走路草/毛球/烈雀 Top3 vs 喇叭芽/凯西/素利普也在池里）。
- 待办（用户未最终确认）：地图弹窗补一行「稀有遭遇」（低权重物种），使两边自洽。

### 19. 技能错乱修复（学招队列按 uid 关联 + 跨级去重）
- 用户复现：皮卡丘首发 → 喂经验给卡比兽连升多级 → 把卡比兽设为首发 → 学招弹窗疯狂弹出且新招错学到皮卡丘身上。
- 根因 1（技能错乱）：待学招队列 `pendingLearn` 只存队伍下标，`setLeadMon` 重排后下标失效；且 `save()` 序列化时丢 uid。修复：宝可梦新增稳定 `uid`（makeMon/序列化/反序列化），待学招按 uid 定位（学招弹窗与 resolve 都改），存档保留 uid；旧档无 uid 的待学招读档时按下标补挂。
- 根因 2（疯狂弹窗）：`grantExp` 每级用 `movesAtLevel`（最近 4 招）判断新招，同一招跨级重复入队（实测一次喂经验攒 100+ 条）。修复：改为「当前等级以下所有未学且未入队的招，每招只入队一次」（保留进化补学）；实测同一场景待学招降到 4 条（睡觉/瞬间失忆/泰山压顶/地震）。
- 提交：`7c636ca`。逻辑 377/377（含错乱回归 8 项），浏览器 77/77。

### 20. 对话存档同步
- 用户要求保存会话供家中 Codex 读取，并随后要求同步/提交/推送；本存档文件随每次会话结束更新。
- 提交：`a2a522a`（首次存档）。

### 21. 进化链祖先点亮
- 用户：拥有喷火龙，是不是图鉴所有升级路径都应该被点亮。
- 处理：`lightUpFamily(id)`——拥有任一进化形（捕获/获得/读档回填/进化时）自动点亮祖先链（等级+石头进化）；旧档直接拥有喷火龙 → 小火龙/火恐龙/喷火龙全点亮；不点亮未获得的后代（避免开局选小火龙剧透喷火龙），石头进化只点亮祖先不误亮兄弟分支。
- 提交：见最新 git log（本条目后的提交）。逻辑 381/381，浏览器 77/77。

---

## 二、当前技术状态（截至本存档，2026-08-14 晚）

- 逻辑冒烟：381/381 通过；浏览器冒烟：77/77 通过；0 控制台错误。
- 最近提交链：`e8be6ae` → `d64cea4` → `872be85` → `b2722d4` → `1c4cabd` → `16b9d7d` → `2d6e1fb` → `4f93c2d` → `4ad31a7` → `1b84e53` → `cb9bacf` → `c2d2574` → `51bf929` → `fa363f2` → `a2a522a` → `7c636ca` → `bdbcfc4` →（进化链祖先点亮提交，见 git log）（main 分支，均已 push）。
- 图鉴 151 只全部可获取；进化/石头进化自动点亮图鉴；旧档读档回填。
- 道具图鉴、招式效果说明、招式更换面板、图鉴详情（属性/技能/获取途径）、队伍条直达详情均已上线。

## 三、待办 / 已知简化（下次可继续）

- 地图弹窗「主要遭遇」与图鉴途径的口径统一（建议：Top3 外补一行稀有遭遇；用户倾向方案 2，未最终确认）。
- 生成图鉴（98 只）学习面为属性模板简化版，未按官方等级表逐只校对。
- 两回合招式（挖洞/飞空/蓄力）暂按单回合处理；速度增益实际已影响先手，但 AGENTS.md 旧备注「暂不影响先手」已过时待清理。
- 音效用户明确不需要，暂缓。
- 成就系统未做（`STATE.titles` 已留口子，无尽之塔称号已写入）。

## 四、在家里的 Codex 读取方式

1. 把本文件放到家中项目目录（或任意位置）；
2. 直接告诉 Codex：「先读 `对话存档_2026-08-14.md`，恢复这个项目（bkmGame 宝可梦游戏）的上下文」；
3. 或把文件内容粘贴进新对话开头作为背景。
4. 家里的项目路径可能不同，先让 Codex 用 `rg --files` 确认 `app.html` / `js/core.js` 等文件位置。
