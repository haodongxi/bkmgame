# 会话总结（2026-08-16 ~ 08-17）

Created by haodongsheng

## 项目一句话

宝可梦：关都篇 —— 纯文字、点击驱动的竖屏 H5 单机 RPG（原生 JS 零依赖，151 只初代宝可梦 + 8 道馆 + 无尽/超越之塔 + 称号 + 专属道具）。

## 异地快速上手

```sh
git clone git@github.com:haodongxi/bkmgame.git   # 或 git pull 拉最新
cd bkmgame

# 逻辑冒烟（无浏览器依赖，秒级）
/Users/haodongsheng/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node test/smoke_test.js

# 浏览器冒烟（需先起无头 Chrome）
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --remote-debugging-port=9222 --user-data-dir=/tmp/bkm-chrome-profile "file:///Users/haodongsheng/git/bkmgame/app.html"
/Users/haodongsheng/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node test/browser_play.js
```

直接玩：浏览器打开 `app.html`（存档在 localStorage，key=`bkm_poke_save_v1`）。

## 本次会话改动总览（全部已提交推送）

### 图鉴与界面
- 图鉴详情返回保留滚动位置；「全部 + 17 属性」二级 Tab（未解锁只出现在全部）；合集 Tab 各自记忆位置（`eba4071`、`2f7132a`）
- 野外操作面板恢复「地图 / 返回城镇 / 前往下个地点」三个独立按钮（原顺序，洞穴隐藏返回城镇）（`5dfc712`、`803bff0`）
- 招式物攻/特攻类别标注：战斗按钮、详情面板、更换招式弹窗统一显示「物攻/特攻/变化 · 属性」（`cf515f6`）
- 电脑箱详情加「← 返回电脑箱」按钮，返回后保留列表滚动位置（`6b8d26c`）
- 电脑箱 HP 条换经验条 → 已按用户要求回退（`a87bbe8` 已 revert，`61286b3`）

### 战斗与数值
- 遗忘技能只影响「升级自动提示」，详情更换招式仍可手动学回（修复旧档「不学了就永久消失」）（`4e5f6f9`、`7ae7815`）
- 先发制人按「对位」重算：敌方换宝可梦后重新比速度，每只新登场宝可梦首回合先手 +5%（`2c8e166`）
- 塔层克制建议：必须对主题内全部属性不弱才推荐，修复 71-80 超能/幽灵/恶层误推格斗（`25e48d5`）

### 电脑箱与经济
- 传送费用：5000 → 2500 → 等级²（最低 1000 金），防低等级倒刷糖果（`9cd6f1c`、`359c935`、`fb97345`）
- 电脑箱锁只限制传送，取回不受锁限制（`56d2358`）

### 专属道具系统（MVP-S1 ~ S5）
- 20 件专属携带物（含电气球），绑定特定宝可梦（支持一家多只）；传说级宝可梦不设专属
- 产出渠道全部 0.1%（千分之一）：隐藏点一次性（城镇闲逛 + 野外探索均可触发）/ 火箭队掉落 / 商人稀有货 / 塔每 5 层奖励 / 钓鱼
- 效果：属性招式 +50%（电/火/水/龙/幽灵/格斗/岩石/冰/虫/毒/普通/飞行）、心灵汤勺特攻 +30%、阳光花环草系 +50% 且晴天再 +50%、秘传之眼/幸运葱必中+暴击翻倍、剩饭盒回合末回血、护士帽恢复道具 ×1.5
- 详情面板显示「（专属）」标记，道具图鉴自动收录
- 修复：非城镇节点（冠军之路/双子岛/道路）隐藏点原先永远无法触发 → 野外探索可翻出（`94d0732`）
- 修复：火箭队抢劫败北不再没收专属道具（防止翻出后永久丢失）（`5cff1f7`）
- 概率 1% → 0.1%（`e2dd0d2`）

### 双塔修复
- 重刷无尽之塔不再丢失超越之塔入口：`cleared` 保持通关标记，新增 `replaying` 状态（`f368630`）
- 旧档追溯：修复前已重刷的存档（bestFloor≥100 或持「无尽之塔征服者」称号）读档自动恢复通关标记（`5c96e30`）
- 双塔交错回归：随时重刷互不干扰、切塔进度保留、重刷中通关状态复位（`29720c4`）

## 当前验证状态

- 逻辑冒烟：543 / 543 通过
- 浏览器冒烟：156 / 156 通过，0 异常 0 控制台错误
- 工作区干净，`main` 与 `origin/main` 同步

## 测试存档（毕业档）

本次会话生成过一份「关都冠军毕业档」存档码（全图鉴、8 徽章、满级队伍带专属道具、双塔通关、全部专属道具 + 称号）。存档码在会话历史中，异地如需测试可用游戏内「导入存档」粘贴，或用 vm 脚本重新生成（构造 STATE 后 `save()` + base64 编码）。

## 已知简化 / 待办

- 两回合招式（挖洞/飞空/蓄力）按单回合处理；日光束晴天免蓄力
- 学习面为简化版，未按官方等级表逐只校准
- 生成招式二次效果未逐条实现（手工精选 165 个已实现）
- 音效未做（用户明确不需要）
- 可选后续：超梦平衡讨论（已回退，未改数值）、速度属性进一步调优、塔刷钱限制等
